#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;
int main(){
string str;
string vs;
cin >> str;
cin >> vs;
int count=0;
vector<int> vc;
for(int i=str.length(); i<vs.length(); i++){
for(int k=0; k<vs.length(); i++){
if(str[k]!=vs[k]){
count++;
}

}
vc.push_back(count);
int count=0;


}
}